#include <bits/stdc++.h>
using namespace std;

double num;

int main() {

    // freopen("Accoder #11015.in", "r", stdin);
    // freopen("Accoder #11015.out", "w", stdout);

    scanf("%lf", &num);

    printf("%.6lf", pow(num, 1.0 / 3));

    // fclose(stdin);
    // fclose(stdout);

    // system("pause");

    return 0;
}