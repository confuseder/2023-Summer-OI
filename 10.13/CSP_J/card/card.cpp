#include <bits/stdc++.h>
using namespace std;

int n;


int main() {

    // freopen("card.in", "r", stdin);
    // freopen("card.out", "w", stdout);

    

    // fclose(stdin);
    // fclose(stdout);

    // system("pause");

    return 0;
}